<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editorial Epectacular</title>
    <%- include("../partials/head") %>
    <link rel="stylesheet" href="/stylesheets/admin.css">
</head>
<body>
    <div class="container p-4">
        <div class="row">
            <div class="col-md-4 offset-md-4">
                <div class="card-body">
                    <form action="/admin/update/<%= fanzine._id %>?_method=PUT" method="POST" enctype="multipart/form-data">

                        <div class="input-group mb-3">
                            <div class="custom-file">
                                <input type="file" name="image" class="custom-file-input" id="inputGroupFile02" value="<%= fanzine.imageURL %>">
                                <label class="custom-file-label" for="inputGroupFile02" aria-describedby="inputGroupFileAddon02">Cargar imagen</label>
                            </div>
                        </div>

                        <h5>Título del Fanzine</h5>
                        <div class="form-group">
                            <textarea id="summernote" name="title" class="form-control" placeholder="Title" value=""><%= fanzine.title %></textarea>
                        </div>

                        <h5>Descripción</h5>
                        <div class="form-group">
                            <textarea cols="38" rows="5" name="description" value=""><%= fanzine.description %></textarea>
                        </div>
                        
                        <div class="form-group">
                            <button type="submit" class="btn btn-success btn-block">
                                Cargar
                            </button>
                        </div>
                        
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        $(document).ready(function() {
            $('#summernote').summernote();
        });
    </script>
</body>
</html>